#include "stdafx.h"

// Adds a tag to the linked list
void AddTag(char* szTag);
// Clears the list
void ClearTags();
// func for stripping right before printing the names
void StripTags(char* buffer, char* szName);